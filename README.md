<p align="center"><img src="icons/icon128.png" width="96" alt="Any Video Download 图标"></p>

# Any Video Download

开源网页视频检测与下载扩展，适用于 Chrome / Edge（Manifest V3）。当前版本 **1.2.7**，采用 [MIT 许可证](LICENSE)。

从网页媒体请求和播放器中发现视频，在侧栏选择清晰度、下载和查看进度。HLS、DASH 及分离音视频通过 Windows 本地 FFmpeg 助手输出 MP4。

## 功能

- 检测普通视频、HLS、DASH，支持 iframe 和开放 Shadow DOM 中的播放器。
- 解析清晰度、时长和独立音轨，将属于同一播放列表的资源归组。
- 监听播放和换源，持续发现广告结束后加载的媒体；明确提示未匹配地址的播放器。
- 关联抖音当前播放器提供的音视频轨，通过本地助手合并。
- 下载记录显示进度、速度、保存路径，支持取消、打开视频、在文件夹中显示和清空已结束记录。
- 记录保存在本机；清空记录不删除已下载的视频，也不清除浏览器的下载历史。

## 安装扩展

1. [下载源码 ZIP](https://github.com/linshu618/any-video-download/archive/refs/heads/main.zip) 并解压到固定目录，或克隆此仓库。
2. Chrome 打开 `chrome://extensions`（Edge 为 `edge://extensions`），开启“开发者模式”。
3. 点击“加载已解压的扩展程序”，选择包含 `manifest.json` 的项目根目录。
4. 刷新视频网页并播放，点击工具栏中的扩展图标打开侧栏。

仓库包含已构建的后台脚本，无需先安装 npm 依赖即可加载扩展。普通视频文件可交给浏览器下载；流媒体下载、打开视频及定位文件需要下面的本地助手。

## 安装本地助手（Windows）

先安装 Node.js（建议 22 或更新的 LTS）以及 FFmpeg，将 `ffmpeg.exe` 和 `ffprobe.exe` 放在同一目录。本项目不附带这些程序。扩展 ID 可在扩展管理页的卡片上找到。

在项目根目录打开 PowerShell，替换示例中的扩展 ID 和 FFmpeg 路径后运行：

```powershell
./native/install.ps1 -ExtensionId <扩展ID> -FfmpegPath 'C:\ffmpeg\bin\ffmpeg.exe'
```

安装脚本仅为当前用户注册 Chrome / Edge 的 Native Messaging 主机，只允许指定扩展调用，不启动监听端口。默认保存到系统“下载”文件夹，也可通过 `-DownloadDir` 指定目录。移动项目目录或更换扩展 ID 后须重新注册，可运行 `./native/uninstall.ps1` 移除注册。

关闭侧栏后下载仍可继续，但重新加载扩展或退出浏览器会中断流媒体任务。请等任务结束后再更新扩展。

## 更新与常见问题

更新源码后，到扩展管理页点击此扩展卡片上的 **↻ 重新加载**，再刷新视频网页、重新打开侧栏。只关闭侧栏并不能保证后台脚本更新。

- **“后台未确认清空操作”**：可能仍在运行旧后台，按上面的步骤重新加载后重试。
- **“下载助手连接失败”**：核对扩展 ID、Node.js 和 FFmpeg 路径，并重新运行安装脚本。
- **下载完成但无法打开**：检查文件是否已移动或删除，以及本地助手是否已安装。
- **播放器有时长却无下载条目**：尚未找到可以确认属于它的媒体地址，不代表已经拿到完整视频。

## 支持边界

- 不保证支持所有网站。DRM、私有传输协议（如 YouTube UMP）不作为可直接下载的 MP4。
- 严格防盗链、需要 Cookie 或自定义 Authorization 头的资源可能解析或下载失败。目前不会导出浏览器 Cookie 给本地助手。
- blob URL 不是文件地址，必须找到其背后的媒体请求或播放器配置。网站变更后可能需要适配。
- 广告和正片可能同时列出；不会仅凭视频时长判断广告，也不保证拆分服务器拼接的广告。
- 直播下载暂未启用。FFmpeg 使用流复制，不兼容 MP4 容器的编码会报错。
- 本地助手当前仅提供 Windows 安装与文件操作支持。

请仅下载自己拥有或获授权保存的内容。

## 隐私与权限

- `webRequest` / 网站访问权限：观察媒体响应，读取当前网页的播放器信息。
- `tabs` / `sidePanel`：将资源关联到标签页并展示侧栏。
- `downloads`：下载普通文件并跟踪进度。
- `storage`：本地保存设置与下载记录，记录不保存媒体签名链接或登录凭据。
- `nativeMessaging`：调用本机 FFmpeg 助手和文件操作。

没有自建上传服务或统计服务。下载会直接请求媒体所在服务器。安装生成的本机配置、下载文件、测试输出和环境变量文件不纳入仓库。

## 开发与验证

```powershell
npm ci
npm run icons
npm run build
npm test
```

后台入口为 `background/main.js`，构建产物为 `background/service-worker.js`。XML 解析依赖随后台打包，不加载远程脚本。图标源文件为 `icons/icon-source.png`，生成 16、32、48、128 像素图标。

配置本地助手后，测试还会使用本地合成媒体验证 FFmpeg 下载、音视频合并及原生消息通信。未配置时相关集成测试会跳过。

## 更新记录

- **1.2.7**：使用带圆形播放标记的新图标；完善开源许可、公共安装文档和清空失败提示。
- **1.2.6**：清空已结束的下载历史，保留进行中的任务和已下载文件。
- **1.2.5**：统一普通下载和本地助手下载的文件打开、定位操作。
- **1.2.4**：支持关联抖音当前播放器的分离音视频轨。
- **1.2.3**：改进播放器时长、资源归属和跨 frame 检测。

## 贡献与许可

欢迎提交 Issue 和 Pull Request。反馈时请说明扩展版本、浏览器版本、复现步骤与已脱敏的错误信息；请勿上传 Cookie、带鉴权参数的媒体链接或私人下载文件。

项目采用 [MIT](LICENSE) 许可证。第三方依赖保留各自许可证，见 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。
